import MainTicketTemplate from 'components/MainTicketTemplate'
import LayoutDashboard from 'components/Template'

export default function Tiket() {
  return (
    <LayoutDashboard>
      <MainTicketTemplate title={'Call Someone'} />
    </LayoutDashboard>
  )
}
